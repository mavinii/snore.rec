 import { ActivityIndicator } from 'react-native';

 export default function Loading() {
    return (
        <ActivityIndicator className='flex-1 justify-center bg-zinc-950 items-center text-lime-300'
        />
    ); 
 }